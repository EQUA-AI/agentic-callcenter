{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "environmentName": {
        "value": "${AZURE_ENV_NAME}"
      },
      "currentUserId": {
        "value": "${AZURE_PRINCIPAL_ID}"
      },
      "location": {
        "value": "${AZURE_LOCATION}"
      },
      "runningOnGh": {
        "value": "${GITHUB_ACTIONS}"
      },
      "openAIName": {
        "value": "${AZURE_OPENAI_NAME}"
      },
      "openAIResourceGroupName": {
        "value": "${AZURE_OPENAI_RG}"
      },
      "emailRecipientAddress": {
        "value": "${EMAIL_RECIPIENT_ADDRESS}"
      },
      "embeddingModel": {
        "value": "${AZURE_OPENAI_EMBEDDING_MODEL=text-embedding-3-large}"
      },
      "openAIModel": {
        "value": "${AZURE_OPENAI_MODEL=gpt-4o}"
      },
      "openAIWhisperModel": {
        "value": "${AZURE_OPENAI_WHISPER_DEPLOYMENT=whisper}"
      },
      "openAIWhisperVersion": {
        "value": "${AZURE_OPENAI_WHISPER_VERSION=2024-02-01}"
      },
      "openAIApiVersion": {
        "value": "${AZURE_OPENAI_API_VERSION=2024-02-01}"
      },
      "searchIndexName": {
        "value": "${AZURE_SEARCH_INDEX_NAME=telcoagents}"
      },
      "foundryEndpoint": {
        "value": "${AZURE_AI_FOUNDRY_ENDPOINT=https://weddingomni.services.ai.azure.com/api/projects/WeddingOmni}"
      },
      "agentId": {
        "value": "${AGENT_ID=asst_khFWOGAwaF7BJ73ecupCfXze}"
      },
      "useFoundryAgent": {
        "value": "${USE_FOUNDRY_AGENT=true}"
      },
      "acsChannelRegistrationId": {
        "value": "${ACS_CHANNEL_REGISTRATION_ID=5b42b7cd-1bfe-4f94-acc9-7f46b19cb5ec}"
      },
      "existingAcsName": {
        "value": "${EXISTING_ACS_NAME=WeddingUS}"
      },
      "existingAcsResourceGroup": {
        "value": "${EXISTING_ACS_RESOURCE_GROUP=WeddingBotUS}"
      },
      "useExistingAcs": {
        "value": "${USE_EXISTING_ACS=true}"
      }
    }
}
